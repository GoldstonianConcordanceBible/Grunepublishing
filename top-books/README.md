# Top Books (Priority DOI + Companion Packs)

This list is the “ship first” set.
Each entry should have:
- book folder in `books/{book_id}/`
- companion pack built + uploaded to Figshare
- DOI recorded in the metadata + README