# Agents (Canon Integration)

## Rule
Each book MAY have an agent pack at:
agents/<series-id>/<book-id>/

## Canon link field (recommended)
In canon metadata (where books are listed), add:
- agents_path: "agents/<series-id>/<book-id>/"

If an agent is missing, omit the field (do not fake links).